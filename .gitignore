# Swift / SwiftPM
mac-app/.build/
mac-app/RealtimeTranslator.app/
mac-app/RealtimeTranslator.dmg
*.xcodeproj
.swiftpm/

# Python
backend/.venv/
**/__pycache__/
*.pyc

# Local env (may contain endpoints/keys)
backend/.env.local
backend/.env.tokyo

# Deploy state (environment-specific, not source)
deploy/.instance-id
deploy/.cloudfront-id

# OS
.DS_Store
deploy/.relay-token
deploy/.instance-id.big
deploy/.golden-ami
