# Realtime Translator — インストール手順（チーム向け）

リアルタイム日本語 ↔ 韓国語 音声翻訳 Mac アプリです。マイク（自分の声）と
システムオーディオ（Zoom の相手・動画の音）を取り込み、画面に翻訳字幕を表示します。

---

## 受け取るもの
- **`RealtimeTranslator.dmg`**（配布者から受領）
- **サーバーアドレス**: `wss://dv7fu8km0bcfp.cloudfront.net` ← アプリに既定で設定済み

> ⚠️ サーバーは**デモ時間中（約5時間）のみ起動**します。それ以外の時間は接続できません。

---

## インストール（初回のみ）

### 1. アプリのインストール
1. `RealtimeTranslator.dmg` をダブルクリック → 開いたウィンドウで **RealtimeTranslator を Applications フォルダへドラッグ**

### 2. 初回起動 —「確認されていない開発元」を許可する
このアプリは社内ツールのため Apple の公式署名がありません。そのため**初回のみ**、以下の操作が必要です:

1. Applications で **RealtimeTranslator をダブルクリック** → 「開けません」という警告 → **キャンセル** を押す
2. **システム設定 ▸ プライバシーとセキュリティ**（System Settings ▸ Privacy & Security）を開く
3. 下にスクロール → **「"RealtimeTranslator" は…ブロックされました」** の横の **「このまま開く（Open Anyway）」** をクリック
4. 再度確認ダイアログが出たら **「開く」** をクリック

> この操作は**初回のみ**です。次回からはダブルクリックでそのまま開きます。

### 3. アクセス許可（アプリで Start を押すと要求されます）
アプリ起動後に **Start** を押すと、macOS が2つの権限を要求します:

| 権限 | 用途 | 操作 |
|---|---|---|
| **マイク（Microphone）** | 自分の声の翻訳 | ポップアップで **「許可」** をクリック |
| **画面収録（Screen Recording）** | システムオーディオ（Zoom の相手・動画）の翻訳 | **システム設定 ▸ プライバシーとセキュリティ ▸ 画面収録** で RealtimeTranslator を **オン** → **アプリを再起動** |

> 画面収録の権限は、オンにした後**アプリを再起動**しないと反映されません。macOS が「終了して再度開く（Quit & Reopen）」ボタンを表示したら押してください。

---

## 使い方

1. アプリ左側のパネルで:
   - **System audio**（システムオーディオ）トグル — Zoom の相手・動画の音を翻訳
   - **Microphone**（マイク）トグル — 自分の声を翻訳
   - 入力 / 出力デバイスの選択（通常は既定値で OK）
2. 言語: **KO ↔ JA**（既定）。中央の ⇄ ボタンで方向を切り替え
3. **Start** をクリック
4. 右側にリアルタイム字幕:
   - **ME**（青いタグ）= 自分のマイク
   - **THEM**（緑のタグ）= システムオーディオ（相手）
   - グレーの文字 = 発話中（暫定）、濃い文字 = 確定した文章

**ヒント:**
- 字幕は**ドラッグしてコピー**できます（他の場所に貼り付け可能）
- **Export .md** ボタンで翻訳記録全体をファイルに保存
- Stop してから再度 Start しても、以前の記録は残ります（Clear で消去）

---

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| 接続できない / 字幕が出ない | サーバーが起動している時間か確認（デモ時間 約5時間のみ稼働） |
| マイクの数値は増えるが翻訳が出ない | サーバーアドレスが `wss://dv7fu8km0bcfp.cloudfront.net` か確認 |
| システムオーディオ（THEM）が入らない | 画面収録の権限をオンにして**アプリを再起動**したか確認 |
| Zoom の相手の声が拾えない | イヤホンのみだと拾えない場合あり — スピーカー/モニター出力に切替 |

---

## 技術情報（参考）
- サーバー: 東京リージョンの GPU（faster-whisper large-v3 + Qwen3-32B）。マイクと
  システムオーディオを**それぞれ独立したストリーム**として認識するため、混ざりません。
- アプリ→サーバー: `wss://`（CloudFront エッジ、暗号化）。接続が切れてもアプリが自動再接続します。
