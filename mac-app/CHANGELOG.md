# Mac 앱 버전 이력

`mac-app/VERSION` 파일의 값이 빌드 시 앱에 박히고(좌측 패널 하단 표시), 빌드
날짜가 자동 첨부됩니다 (`bundle.sh`). 기능을 추가하면 **VERSION을 올리고 여기에
한 줄 적는다.** 그러면 받는 사람이 앱 하단에서 자기 버전을 확인할 수 있다.

> 버전을 올리는 곳: `mac-app/VERSION` (예: `v1.3` → `v1.4`).
> shared/public dmg는 자동 갱신 안 됨 — 버전 올리면 `package-dmg.sh`로 재빌드.

---

## v1.3 — 2026-06-21
- **지난 회의 기록 보기**: 앱에서 `/history` 페이지로 이동하는 버튼. 자기 방의
  끝난 세션들의 전체 전사 + 자동 요약/핵심포인트/다음액션을 브라우저에서 열람.
- **앱 버전 표시**: 좌측 패널 하단에 `버전 (빌드날짜)`.

## v1.2 — 2026-06-21
- **방(room) 격리 + 방 비번**: 같은 서버를 여러 명이 써도 방이 다르면 자막 격리.
  민감한 회의는 방 비번(opt-in)으로 잠금 — 비번 아는 사람만 시청/기록 열람.
- **설정 영구 저장**: 서버 주소·방·방비번·언어쌍·오디오 선택이 재실행에도 유지.
- **서버 끄기 UI 제거**: 공유 릴레이라 항상 켜둠 (사용자가 못 끔).

## v1.1 — 2026-06-20
- **라이브 인사이트(회의 코파일럿)**: 컨텍스트 기반 실시간 요약 + 추천 질문,
  마무리 정리(핵심+다음액션). 한국어 출력. 토글 OFF면 호출 0(비용 0).
- **드래그 전체 선택**: 전사·인사이트 영역을 한 번에 드래그/복사.
- **Claude(Bedrock) 번역 토글**, **구두점 문장끊기 + 영어 어순 게이트**.
- 배포 빌드: ad-hoc 서명(개인 신원 미포함) + 기본 서버주소 빈칸(공개) /
  내 박스 주소 주입(shared).

## v1.0 — 그 이전
- KO↔JA 실시간 음성 번역, 시스템오디오+마이크 2스트림, 브로드캐스트 뷰어,
  도쿄 GPU 박스(자동끔→이후 상시), Wake & Start.
