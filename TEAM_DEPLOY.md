# Realtime Translator — 팀원용 설치 안내

실시간 한국어 ↔ 일본어 음성 번역 Mac 앱입니다. 마이크(내 목소리)와
시스템 오디오(Zoom 상대방·영상 소리)를 받아 화면에 번역 자막을 띄웁니다.

---

## 받는 것
- **`RealtimeTranslator.dmg`** (배포자가 전달)
- **서버 주소**: `wss://dv7fu8km0bcfp.cloudfront.net`  ← 앱에 이미 기본 설정됨

> ⚠️ 서버는 **데모 시간(약 5시간)에만 켜집니다.** 그 외 시간엔 연결이 안 됩니다.

---

## 설치 (1회)

### 1. 앱 설치
1. `RealtimeTranslator.dmg` 더블클릭 → 열린 창에서 **RealtimeTranslator를 Applications 폴더로 드래그**

### 2. 첫 실행 — "확인되지 않은 개발자" 통과하기
이 앱은 사내 도구라 Apple 공식 서명이 없습니다. 그래서 **처음 한 번만** 아래가 필요합니다:

1. Applications에서 **RealtimeTranslator 더블클릭** → "확인할 수 없어 열 수 없습니다" 경고가 뜸 → **취소** 누름
2. **시스템 설정 ▸ 개인정보 보호 및 보안** 열기
3. 아래로 스크롤 → **"RealtimeTranslator을(를) 차단했습니다"** 옆의 **"확인 없이 열기(Open Anyway)"** 클릭
4. 다시 한 번 확인 창이 뜨면 **"열기"** 클릭

> 이 과정은 **처음 한 번만** 하면 됩니다. 그 다음부터는 더블클릭으로 바로 열립니다.

### 3. 권한 허용 (앱에서 Start 누르면 요청됨)
앱 실행 후 **Start**를 누르면 macOS가 두 가지 권한을 요청합니다:

| 권한 | 용도 | 방법 |
|---|---|---|
| **마이크** | 내 목소리 번역 | 팝업에서 **"허용"** 클릭 |
| **화면 기록** | 시스템 오디오(Zoom 상대방·영상) 번역 | **시스템 설정 ▸ 개인정보 보호 및 보안 ▸ 화면 기록**에서 RealtimeTranslator **켜기** → **앱 재시작** |

> 화면 기록 권한은 켠 뒤 **앱을 껐다 켜야** 적용됩니다. macOS가 "종료 후 다시 열기" 버튼을 주면 누르세요.

---

## 사용법

1. 앱 왼쪽 패널에서:
   - **System audio**(시스템 오디오) 토글 — Zoom 상대방·영상 소리 번역
   - **Microphone**(마이크) 토글 — 내 목소리 번역
   - 입력/출력 장치 선택 (보통 기본값으로 OK)
2. 언어: **KO ↔ JA** (기본). 가운데 ⇄ 버튼으로 방향 전환
3. **Start** 클릭
4. 오른쪽에 실시간 자막:
   - **ME**(파란 태그) = 내 마이크
   - **THEM**(초록 태그) = 시스템 오디오(상대방)
   - 회색 글씨 = 말하는 중(임시), 진한 글씨 = 확정된 문장

**팁:**
- 자막은 **드래그해서 복사** 가능 (다른 곳에 붙여넣기)
- **Export .md** 버튼으로 전체 번역 기록을 파일로 저장
- Stop 했다 다시 Start 해도 이전 기록은 남아있음 (Clear로 지움)

---

## 문제 해결

| 증상 | 해결 |
|---|---|
| 연결 안 됨 / 자막 안 뜸 | 서버가 켜져 있는 시간인지 확인 (데모 시간 ~5h만 운영) |
| 마이크 숫자는 올라가는데 번역 안 뜸 | 서버 주소가 `wss://dv7fu8km0bcfp.cloudfront.net` 맞는지 확인 |
| 시스템 오디오(THEM) 안 들어옴 | 화면 기록 권한 켜고 **앱 재시작**했는지 확인 |
| Zoom 상대방 소리가 안 잡힘 | 이어폰만 쓰면 안 잡힐 수 있음 — 스피커/모니터 출력으로 |

---

## 기술 정보 (참고)
- 서버: 도쿄 리전 GPU (faster-whisper large-v3 + Qwen3-32B). 마이크/시스템을
  각각 독립 스트림으로 인식해 섞이지 않음.
- 앱→서버: `wss://`(CloudFront 엣지, 암호화). 연결 끊겨도 앱이 자동 재연결.
