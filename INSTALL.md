# 설치 가이드 — Realtime Translator (Mac 앱)

실시간 음성 번역 Mac 앱입니다. 마이크(내 목소리)와 시스템 오디오(상대방·영상
소리)를 받아 화면에 번역 자막을 띄우고, 회의 요약·다음 액션도 정리해 줍니다.

이 문서는 **앱을 받은 사람**을 위한 것입니다. 두 가지 경우가 있습니다:

- **A. 내 서버를 직접 운영하려는 경우** — 자기 AWS 계정에 GPU 박스를 배포하고
  그 주소를 앱에 넣어 씁니다. → [A 섹션](#a-내-서버를-직접-운영)
- **B. 받은 서버를 그대로 쓰는 경우** — 배포자가 운영 중인 박스를 쓰며, 앱에는
  주소가 이미 들어 있고 **비밀번호만** 입력하면 됩니다. → [B 섹션](#b-받은-서버를-그대로-사용)

> 어느 경우든 **앱 설치 + 권한 허용**은 똑같습니다. 먼저 아래 공통 설치를 하세요.

---

## 공통: 앱 설치 (1회)

### 0. 요구 사항
- **macOS 14 (Sonoma) 이상** — 시스템 오디오 캡처에 필요.

### 1. 앱 설치
1. 받은 `.dmg`(`RealtimeTranslator-public.dmg` 또는 `-shared.dmg`)를 더블클릭
2. 열린 창에서 **RealtimeTranslator를 Applications 폴더로 드래그**

### 2. 첫 실행 — Gatekeeper("확인되지 않은 개발자") 통과
이 앱은 Apple 공증을 받지 않았습니다(개인/사내 도구). 그래서 **처음 한 번만**:

1. Applications에서 **RealtimeTranslator 더블클릭** → 차단 경고가 뜨면 **취소**
2. **시스템 설정 ▸ 개인정보 보호 및 보안** 열기
3. 아래로 스크롤 → **"RealtimeTranslator을(를) 차단했습니다"** 옆 **"확인 없이 열기"** 클릭
4. 다시 뜨는 확인 창에서 **"열기"**

> 또는: Finder에서 앱 **우클릭 ▸ 열기 ▸ 열기**.
> "손상되어 열 수 없습니다"가 뜨면(다운로드 격리 표식 때문) 터미널에서:
> `xattr -dr com.apple.quarantine /Applications/RealtimeTranslator.app`
>
> 모두 **처음 한 번만** 하면 됩니다.

### 3. 권한 허용 (앱에서 Start 누를 때 요청됨)
| 권한 | 용도 | 방법 |
|---|---|---|
| **마이크** | 내 목소리 번역 | 팝업에서 **"허용"** |
| **화면 기록** | 시스템 오디오(상대방·영상) 번역 | 시스템 설정 ▸ 개인정보 보호 및 보안 ▸ **화면 기록**에서 RealtimeTranslator **켜기** → **앱 재시작** |

> 화면 기록은 켠 뒤 **앱을 껐다 켜야** 적용됩니다.

---

## A. 내 서버를 직접 운영

자기 AWS 계정에 GPU 박스를 띄우고 그 주소로 연결합니다. (소스 저장소 필요 —
README의 "Deploy the GPU box" 참고.)

1. **인프라 배포** (저장소에서):
   ```bash
   aws sso login                                   # 자기 계정 로그인
   echo "$(openssl rand -hex 12)" > deploy/.relay-token   # 접속 비밀번호 생성
   deploy/launch.sh                                # GPU 박스 + IAM/SG/EIP 생성
   deploy/wake-deploy.sh                           # wake Lambda + CloudFront 연결
   ```
   끝나면 **CloudFront 주소**(`wss://xxxx.cloudfront.net`)와 **비밀번호**
   (`deploy/.relay-token` 내용)가 나옵니다.
2. **앱에 입력**:
   - **Server**: 위 CloudFront 주소
   - **접속 비밀번호**: `deploy/.relay-token` 값
3. 한 번 넣으면 저장돼서 다음 실행에도 유지됩니다.
4. **Wake & Start** → 박스가 깨어나고(최대 ~6분, 모델 로딩) 준비되면 자동 시작.

> ⚠️ 박스는 **15분 유휴 시 자동으로 꺼집니다**(과금 절약). 다시 쓸 땐 Wake & Start.
> ⚠️ 보안: 박스는 `RT_RELAY_TOKEN`이 설정돼 있어야 외부 접속을 받습니다. 토큰
> 없이 공개 인터페이스에 띄우면 **모든 연결을 거부**합니다(fail-closed). 의도적
> 무인증은 `RT_ALLOW_OPEN=1`로만 가능(권장하지 않음).

---

## B. 받은 서버를 그대로 사용

배포자가 운영하는 박스를 씁니다. 받은 `.dmg`가 **`-shared`** 버전이면 서버 주소가
이미 들어 있습니다.

1. **Server**: `-shared` 빌드면 이미 채워져 있음. (`-public` 빌드를 받았다면
   배포자가 알려준 `wss://...` 주소를 직접 입력.)
2. **접속 비밀번호**: 배포자에게 받은 비밀번호를 입력. (보안상 앱에는 안 들어
   있습니다 — 한 번 입력하면 저장돼 유지됩니다.)
3. **Wake & Start** → 박스가 자고 있으면 깨우고(최대 ~6분), 준비되면 자동 시작.

> 박스가 꺼져 있을 수 있습니다(유휴 자동 종료). Wake & Start가 알아서 깨웁니다.
> 비밀번호가 틀리면 연결이 거부됩니다(자막 안 뜸).

---

## 사용법 (공통)

1. 왼쪽 패널: **System audio**(상대방·영상), **Microphone**(내 목소리) 토글, 장치 선택
2. **Languages**: 번역 언어쌍 선택(KO/JA/EN), 가운데 ⇄로 방향 전환. 선택은 저장됨.
3. **Start**(또는 **Wake & Start**) → 오른쪽에 실시간 자막
   - **ME**(파랑) = 내 마이크, **THEM**(초록) = 시스템 오디오
   - 회색 = 말하는 중, 진하게 = 확정 문장
4. (선택) **라이브 인사이트**: 토글을 켜고 컨텍스트(내 역할·관점)를 적으면 대화
   요약 + 추천 질문을 실시간으로, "마무리 정리"로 핵심·다음 액션을 만들어 줍니다.
   끄면 호출이 없어 추가 비용 0.
5. 자막/요약은 **드래그로 전체 선택·복사**, **Export .md**로 저장 가능.

설정(서버 주소·비밀번호·언어쌍·오디오 선택·인사이트 컨텍스트 등)은 모두 저장되어
다음 실행에도 유지됩니다.

---

## 문제 해결

| 증상 | 해결 |
|---|---|
| 연결 안 됨 / "unauthorized" | 비밀번호가 맞는지 확인. 서버 주소(`wss://...`) 확인. |
| 자막이 한참 안 뜸 | 박스가 자고 있을 수 있음 → **Wake & Start**로 깨우고 ~6분 대기. |
| 시스템 오디오(THEM)가 안 잡힘 | 화면 기록 권한 켜고 **앱 재시작**했는지 확인. 이어폰만 쓰면 안 잡힐 수 있음. |
| 재빌드할 때마다 권한이 풀림 | 배포용 .dmg는 ad-hoc 서명이라 그럴 수 있음. 직접 빌드 시 Apple Development 서명(무료 Apple ID로도 가능)을 쓰면 권한이 유지됨. |
